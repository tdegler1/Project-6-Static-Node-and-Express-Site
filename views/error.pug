extends layout.pug

block content
    nav.grid-container.portfolio-breadcrumb
        a(href='/') ← Back
    div.error-message
        h1= error.message
        h2= error.status
    div.error-message
        pre= error.stack
    nav.grid-container.portfolio-breadcrumb
        a(href='/') ← Back
